name: reusable-workflow-caller

on:
  push:
    branches: none # [ main ]
  pull_request:
    branches: none # [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  call-workflow-A:
    uses: akramewu/reusable-workflow/.github/workflows/reusable-workflow-called.yml@main
          
    with:
      username: Houssem
    secrets:
      token: ${{ secrets.TOKEN }}

  call-workflow-B:
    uses: akramewu/reusable-workflow/.github/workflows/reusable-workflow-called.yml@main
    with:
      username: Dellai
    secrets:
      token: ${{ secrets.TOKEN }}
